{
  "name": "robots",
  "version": "0.0.0",
  "description": "robots",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bimap": "0.0.15"
  }
}
